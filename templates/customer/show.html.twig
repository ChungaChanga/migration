{% extends 'base.html.twig' %}

{% block title %}Customer{% endblock %}

{% block body %}
    <h1>Customer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ customer.id }}</td>
            </tr>
            <tr>
                <th>SourceId</th>
                <td>{{ customer.sourceId }}</td>
            </tr>
            <tr>
                <th>TransferStatus</th>
                <td>{{ customer.transferStatus.value }}</td>
            </tr>
            <tr>
                <th>DestId</th>
                <td>{{ customer.destId }}</td>
            </tr>
            <tr>
                <th>TransferData</th>
                <td>{{ customer.transferData }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_customer_index') }}">back to list</a>

    <a href="{{ path('app_customer_edit', {'id': customer.id}) }}">edit</a>

    {{ include('customer/_delete_form.html.twig') }}
{% endblock %}
